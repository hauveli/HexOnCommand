Adds a single command intended for use in datapacks alongside one attribute `hexoncommand:command_permission`

Example usage:

```
/attribute @p hexoncommand:command_permission base set 1
/hexcastinggui
```

Todo:
Maybe add parsing of patterns from commands and adding iotas to stack.
I am unlikely to do this because [mediatransport](https://github.com/penguinencounter/mediatransport) is a perfect solution for my use-case, and may be for you as well.